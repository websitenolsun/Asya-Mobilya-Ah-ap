Place your hero image here with the filename `stairs.jpg`.

Expected path (served at runtime): `/media/hero/stairs.jpg`

Recommended steps:
1. Copy the image you attached in the chat into this folder and name it `stairs.jpg`.
2. Start the dev server for the `ahsap-atelier` app (e.g., `pnpm install` then `pnpm dev` in the `artifacts/ahsap-atelier` folder).
3. Open the homepage to verify the hero image appears below the overlays.

If you'd like, upload the image here and I can add it to the project for you.